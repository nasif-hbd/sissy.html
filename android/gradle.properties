# AndroidX is the modern support library; the build refuses to resolve
# dependencies without this flag.
android.useAndroidX=true

# 2 GB is enough for a project this small and keeps it building on a laptop.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
